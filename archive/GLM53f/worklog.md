# Worklog

---
Task ID: 1
Agent: main (Super Z)
Task: Игра «Крибил» — бильярдный командный пазл (Next.js): физика, NPC-боты, солвер, unit-тесты, UI (планировщик + реалтайм)

Work Log:
- Загружен skill fullstack-dev, инициализировано окружение (Next.js 16 + TS + Tailwind + shadcn/ui), установлен vitest
- Физическое ядро `src/lib/kribil/`: types, vec, geometry (ghost ball, LOS, зеркала бортов), world (фикс. шаг 1/240, трение, упругие столкновения равных масс, борта, КТ по порядку/вразнобой), simulate (детерминированная, кадры для реплея)
- Солвер: CEM (кросс-энтропийный метод) с элитизмом, сглаженным сдвигом среднего, chunk-режимом для UI и локальной полировкой; лосс = время финиша + штраф за проход КТ «по краю» (cpMiss → устойчивые планы)
- Исправленные баги: wrap угла в startParams (отрицательные углы клампились в 0 → «фантомный» best), дубликат evalParams, рассинхрон dt поиска/финала, авто-завершение allStopped в LiveGame при disabled-ударах (добавлен флаг autoAllStopped)
- 4 NPC-бота: Снайпер (ghost-выстрел по чистой линии), Бортовик (рикошет методом зеркала), Таран (сразу и сильно), Тактик (мини-CEM на текущую позицию, 12/12 надёжность после ghost-инициализации)
- LiveGame: живая партия, ручные удары fireShot (только из неподвижного битка), опрос ботов каждые 0.3 с игрового времени
- 25 unit-тестов (vitest): биток попадает по шар1 (+ негативный), шар1 проходит КТ (порядок/вразнобой/недолёт), общее время финиша (скриптованный 4-КТ заезд + детерминизм побитово), трение/упругий удар/борта, отложенный удар движущегося битка, солвер (1 КТ, полный 4×(t,α,P) план, детерминизм seed), planLoss, боты (снайпер/бортовик через борт/таран/тактик), живая партия ботов
- UI: renderer (стол, КТ с пульсацией, стрелки плана, прицел с призрачным шаром и предсказанием направления шар1, след траектории), TableCanvas (rAF, HiDPI, pointer→координаты), PlannerPanel (t/угол/сила per player, drag-прицеливание по выбранному игроку, порядок КТ, перемешать), RealtimePanel (роль: игрок 1-4 / наблюдатель, статусы ботов), KribilGame (режимы, скорость ×0.5–×4, автосолвер с прогрессом, рекорд в localStorage через useSyncExternalStore, журнал событий, диалог «Правила и математика»)
- React Compiler (Next 16): убраны все useCallback, ref-записи вынесены в useLatestRef (эффекты), рекорд через useSyncExternalStore
- Верификация agent-browser: симуляция дефолтного плана (финиш 6.46 с, все КТ, рекорд), авто-расчёт (улучшил до 6.34 с), реалтайм с ботами (статусы, журнал), drag-удар человеком (сила 754, шар1 попал), завершение по allStopped и по таймауту 90 с, мобильная вёрстка 390px, диалог правил; консоль/ошибки — чисто; lint — чисто; тесты 25/25
- Отладочные скрипты удалены, оставлен scripts/solve-default.ts (подбор дефолтного плана)

Stage Summary:
- Дефолтная раскладка решаема: дефолтный план 6.46 с (захардкожен), авто-солвер стабильно находит ~6.3 с за ~2-4 с работы
- Команды: bun run test (vitest 25/25), bun run lint (чисто), dev-server на :3000
- Ключевые файлы: src/lib/kribil/{types,vec,geometry,world,simulate,setup,solver,bots,live,renderer}.ts, src/components/kribil/{TableCanvas,PlannerPanel,RealtimePanel,KribilGame}.tsx, src/app/page.tsx, src/lib/kribil/__tests__/{physics,bots-solver}.test.ts
